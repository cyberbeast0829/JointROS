# 导出描述符原始字节（路线 B：导出的就是缓存里那份**原始 JSON**）。
#
# 产线预烧 / 离线检查用：把导出的字节用 ImportDescriptor 灌到另一台设备/另一台机器。

string bus
---
bool success
string message
uint8[] data                       # 原始 JSON 字节（与 ImportDescriptor.data 成对）
uint16 crc                         # 设备给的描述符 VersionCRC（0 = 未知/来自缓存）
                                  # ⚠ uint16：与 SDK 的 jsdk_desc_hint_t.crc 同宽，不要放宽
uint32 fw_version
