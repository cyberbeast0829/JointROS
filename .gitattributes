# 本仓库在 Windows 上编辑、在 Linux（WSL/容器）上构建 —— 换行符必须统一。
#
# ⚠ 真实踩过的坑：Windows 上用默认设置 checkout 出来的 `.sh` 带 CRLF，
#   在容器/WSL 里执行会报 `set: invalid option` / `'\r': command not found`，
#   而且报错位置与真实原因毫不相干。
#
# 规则：所有文本文件在**工作区**里也统一用 LF（eol=lf），
# 这样 `docker/run.sh`、`tools/build_dev.sh` 在两边都能直接跑。
* text=auto eol=lf

# 显式再声明一遍关键类型（防止将来的规则覆盖）
*.sh           text eol=lf
*.bash         text eol=lf
Dockerfile     text eol=lf
*.cmake        text eol=lf
CMakeLists.txt text eol=lf
*.hpp          text eol=lf
*.cpp          text eol=lf
*.h            text eol=lf
*.c            text eol=lf
*.py           text eol=lf
*.msg          text eol=lf
*.srv          text eol=lf
*.xml          text eol=lf
*.md           text eol=lf
